[whisper]
model_size = "base"  # tiny, base, small, medium, large-v2, large-v3
device = "cpu"       # cpu or cuda
compute_type = "int8"
language = "en"

[audio]
# Uncomment to specify a specific audio device index
# device = 0

[agent]
# Custom prompt template (optional)
# Uses {text} placeholder for the transcribed text
# prompt_template = """..."""
